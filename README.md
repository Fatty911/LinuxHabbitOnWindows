# LinuxHabbitOnWindows
Use your favorite Linux commands to manage **Windows Native Resources**.   Forget `Get-NetTCPConnection`, just type `lsof`.  这是一个为习惯 Linux 命令行的程序员设计的 PowerShell 模块。它不仅仅是别名（Alias），而是将 Linux 命令的逻辑**映射**到 Windows 的底层 API。
